This public-facing repository contains safe-to-share resources used by internal automation tools.
It enables external access to configuration files (e.g., Wi-Fi XMLs) referenced in scripts within the private it-automation repository.
No client data or credentials are stored here.
